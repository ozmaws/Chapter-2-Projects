# Request the inputs
edge = int(input("Enter the length of the edge: "))

# Compute the surface area
surfaceArea = 6 * edge ** 2

# Display the surface are
print("The surface are is " + str(surfaceArea))

